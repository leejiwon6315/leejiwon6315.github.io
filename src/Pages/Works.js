import React from "react";
import styles from "./PageStyles/WorkPage.module.scss";

const Works = ({ name }) => {
  return (
    <>
      <h1 className={styles.ready}> 준비중..</h1>
    </>
  );
};

export default Works;
